# 周报停发名单（管理员设置）——一行一个企微 userid，# 注释与空行忽略。
# 运营裁定（2026-09-05）：教师侧不提供自行停止周报的路径，停发由管理员执行：
#   ① HERMES_HOME=~/.hermes/profiles/lt-tutor hermes cron remove <job_id>（停现有任务）
#   ② 把该教师企微 userid 加入本文件（巡检每日 03:17 读取，不再自动开办）
# 恢复：删掉对应行即可，次日巡检自动重建（provision 脚本幂等，也可手工立即重建）。
