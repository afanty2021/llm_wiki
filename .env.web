# Layer 5 web 同源部署：相对 fetch。
# VITE_API_BASE_URL 置空 → resolveApiBase("") 保留空串 → 同源（/api/v1/... 相对当前源）。
# VITE_USE_HTTP_API 不再需要（fs.ts 改用 caps.platform 运行时判断，见 Task 5）。
# 仅 `--mode web`（npm run build:web）加载此文件；桌面 production（npm run build）不加载，两种构建隔离。
VITE_API_BASE_URL=
